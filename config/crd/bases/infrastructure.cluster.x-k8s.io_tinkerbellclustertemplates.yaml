---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.20.1
  name: tinkerbellclustertemplates.infrastructure.cluster.x-k8s.io
spec:
  group: infrastructure.cluster.x-k8s.io
  names:
    categories:
    - cluster-api
    kind: TinkerbellClusterTemplate
    listKind: TinkerbellClusterTemplateList
    plural: tinkerbellclustertemplates
    singular: tinkerbellclustertemplate
  scope: Namespaced
  versions:
  - name: v1beta2
    schema:
      openAPIV3Schema:
        description: |-
          TinkerbellClusterTemplate is the Schema for the tinkerbellclustertemplates API.
          It is consumed by the Cluster API topology controller to stamp out TinkerbellClusters
          for ClusterClass-based clusters; CAPT itself does not reconcile it.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: TinkerbellClusterTemplateSpec defines the desired state of
              TinkerbellClusterTemplate.
            properties:
              template:
                description: |-
                  TinkerbellClusterTemplateResource describes the TinkerbellCluster stamped out from the template.

                  The full TinkerbellClusterSpec is embedded: ControlPlaneEndpoint is optional and derived by the
                  controller from the owning Cluster, so it is harmless (and conventionally present) in a template.
                properties:
                  metadata:
                    description: Standard object's metadata applied to TinkerbellClusters
                      created from this template.
                    minProperties: 1
                    properties:
                      annotations:
                        additionalProperties:
                          type: string
                        description: |-
                          annotations is an unstructured key value map stored with a resource that may be
                          set by external tools to store and retrieve arbitrary metadata. They are not
                          queryable and should be preserved when modifying objects.
                          More info: http://kubernetes.io/docs/user-guide/annotations
                        type: object
                      labels:
                        additionalProperties:
                          type: string
                        description: |-
                          labels is a map of string keys and values that can be used to organize and categorize
                          (scope and select) objects. May match selectors of replication controllers
                          and services.
                          More info: http://kubernetes.io/docs/user-guide/labels
                        type: object
                    type: object
                  spec:
                    description: Spec is the specification of the desired behavior
                      of the cluster.
                    properties:
                      controlPlaneEndpoint:
                        description: |-
                          ControlPlaneEndpoint is the address of the cluster control plane.
                          When not set, it is populated from the owning Cluster's spec.

                          See https://cluster-api.sigs.k8s.io/developer/architecture/controllers/cluster.html
                          for more details.
                        minProperties: 1
                        properties:
                          host:
                            description: host is the hostname on which the API server
                              is serving.
                            maxLength: 512
                            minLength: 1
                            type: string
                          port:
                            description: port is the port on which the API server
                              is serving.
                            format: int32
                            maximum: 65535
                            minimum: 1
                            type: integer
                        type: object
                      templateInline:
                        description: |-
                          TemplateInline is an inline Tinkerbell Template definition applied to all machines in the
                          cluster. If a Machine specifies its own template, the Machine's template takes precedence.
                          Mutually exclusive with TemplateRef.
                        type: string
                      templateRef:
                        description: |-
                          TemplateRef is a reference to an existing Tinkerbell Template object whose spec.data will
                          be used as the template for all machines in the cluster. If a Machine specifies its own
                          template, the Machine's template takes precedence. Mutually exclusive with TemplateInline.
                          When Namespace is omitted, defaults to the Hardware's namespace.
                        properties:
                          name:
                            description: Name is the name of the object.
                            type: string
                          namespace:
                            description: Namespace is the namespace of the object.
                            type: string
                        required:
                        - name
                        type: object
                    type: object
                    x-kubernetes-validations:
                    - message: templateInline and templateRef are mutually exclusive
                      rule: '!has(self.templateInline) || !has(self.templateRef)'
                required:
                - spec
                type: object
            required:
            - template
            type: object
        type: object
    served: true
    storage: true
